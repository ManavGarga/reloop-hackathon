from fastapi import APIRouter

router = APIRouter()

# TODO (Person 1): Claude-powered chat / AI assistant
# POST /api/ai/chat           → conversational assistant
# POST /api/ai/analyze-return → analyse return reason + suggest action
# POST /api/ai/grade-item     → grade refurbished item condition
